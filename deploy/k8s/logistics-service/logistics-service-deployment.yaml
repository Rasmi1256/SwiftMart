apiVersion: apps/v1
kind: Deployment
metadata:
  name: logistics-service
  labels:
    app: logistics-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: logistics-service
  template:
    metadata:
      labels:
        app: logistics-service
    spec:
      containers:
      - name: logistics-service
        image: swiftmart/logistics-service:latest
        ports:
        - containerPort: 3009
        livenessProbe:
          httpGet:
            path: /health/liveness
            port: 3009
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/readiness
            port: 3009
          initialDelaySeconds: 5
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /health/liveness
            port: 3009
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 30
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: topology.kubernetes.io/zone
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app: logistics-service
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                app: logistics-service
            topologyKey: kubernetes.io/hostname
